Place the privately-held `@p-stream/providers` package in `private/p-stream-providers/`.

Keep its original package name and exports unchanged so the stock P-Stream frontend can consume it without source changes.
