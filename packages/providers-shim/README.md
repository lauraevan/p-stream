# Synapse Player provider adapter

When no authorized provider endpoints are configured, Synapse Player exposes a built-in diagnostic source using a public-domain HLS test stream. This exists only to verify the player/HLS pipeline and must not be treated as the selected title's media source.
